<?xml version="1.0" encoding="utf-8"?>
<!--
  FNASHHA — Android Network Security Config
  ===========================================
  Save this file to:
    android/app/src/main/res/xml/network_security_config.xml

  Then reference it in AndroidManifest.xml inside <application>:
    android:networkSecurityConfig="@xml/network_security_config"

  This config enforces HTTPS-only for production.
  Add your production domain to the <domain> entries below.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">your-fnashha-api.replit.app</domain>
    </domain-config>
    -->
</network-security-config>
