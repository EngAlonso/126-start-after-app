// Placeholder smoke test — the default counter test doesn't apply once
// main.dart boots the real app shell. Feature tests belong alongside each
// feature once screens beyond the Phase 1 foundation exist.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
